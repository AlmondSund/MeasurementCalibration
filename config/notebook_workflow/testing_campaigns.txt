# Held-out deployment campaign that matches the supported production
# configuration. ``MeasurementCalibration`` is trained on so the artifact
# support envelope includes this deployment regime.
test-calibration
